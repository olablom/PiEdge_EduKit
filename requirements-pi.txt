# Pi requirements for PiEdge EduKit
# Install onnxruntime FIRST to avoid NumPy ABI conflicts
# Then install other dependencies

# Core ML runtime (install first!)
onnxruntime>=1.15.0,<1.17.0

# Basic dependencies (pinned for Pi compatibility)
numpy>=1.24.0,<1.27.0
Pillow>=9.5.0,<11.0.0
matplotlib>=3.7.0,<3.9.0
pandas>=2.0.0,<2.2.0
tqdm>=4.65.0,<4.67.0

# System monitoring
psutil>=5.9.0,<6.0.0

# GPIO control (Linux only, falls back to MockGPIO)
RPi.GPIO>=0.7.1; sys_platform == "linux"

