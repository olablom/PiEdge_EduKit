# filename: requirements-minimal.txt
# PiEdge EduKit - Minimal requirements for core micro-lesson (Python 3.12)

# Core ML dependencies
numpy==1.26.4
torch==2.3.1
torchvision==0.18.1
onnx==1.16.1
# Install ORT CPU everywhere EXCEPT Apple Silicon (PEP 508: avoid 'not (...)')
onnxruntime==1.18.0; platform_system != "Darwin" or platform_machine != "arm64"
# Use silicon build on Apple Silicon
onnxruntime-silicon==1.18.0; platform_system == "Darwin" and platform_machine == "arm64"

# Data processing
Pillow==10.3.0
pandas==2.2.2
scikit-learn==1.4.2
matplotlib==3.8.4

# Progress bars
tqdm==4.66.4

# System info
psutil==5.9.8
